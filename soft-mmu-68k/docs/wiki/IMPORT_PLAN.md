# Wiki Import Plan

The restored GitHub Wiki should be imported into this directory so the main repository tracks the public Wiki Markdown.

Import target:

- `soft-mmu-68k/docs/wiki/`

Policy:

- Import all current Wiki `.md` pages.
- Preserve `README.md` as repo-only publishing documentation.
- Review imported pages before publishing back to the Wiki.
- Keep the publish workflow non-destructive.
